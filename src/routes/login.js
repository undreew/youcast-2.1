import {ROUTE_PATH} from 'constants/routes';
import Login from 'views/login/index';

const login = {
	// path: '/login',
	path: ROUTE_PATH.LOG_IN.INDEX,
	element: <Login />,
};

export default login;
